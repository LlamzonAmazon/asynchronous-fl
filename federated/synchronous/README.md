# Synchronous Federated Learning
> This is a baseline for comparison with asynchronous federated learning.

## Walkthrough & Results

```zsh
# Server starts


# Clients connect to server
CLIENT 0: CONNECTING TO SERVER (localhost:8080)...
CLIENT 1: CONNECTING TO SERVER (localhost:8080)...
CLIENT 2: CONNECTING TO SERVER (localhost:8080)...
CLIENT 3: CONNECTING TO SERVER (localhost:8080)...
CLIENT 4: CONNECTING TO SERVER (localhost:8080)...
```